{
  "name": "highlight-simple",
  "version": "1.0.0",
  "description": "Highlight all occurrences of the selected text in the editor",
  "main": "./lib/main",
  "license": "MIT",
  "keywords": [
    "highlight",
    "selection"
  ],
  "repository": "https://github.com/asiloisad-pulsar/highlight-simple",
  "configSchema": {
    "minimumLength": {
      "order": 1,
      "description": "Minimum number of characters in selection before highlights are shown",
      "type": "integer",
      "default": 2
    },
    "maximumHighlights": {
      "order": 2,
      "description": "For performance purposes, the number of highlights is limited. Set to 0 for unlimited.",
      "type": "integer",
      "default": 0,
      "minimum": 0
    },
    "timeout": {
      "order": 3,
      "title": "Debounce Timer",
      "description": "Debounce timer in ms before searching; resets on each selection change",
      "type": "integer",
      "default": 20
    }
  },
  "engines": {
    "atom": ">=1.100.0 <2.0.0"
  },
  "providedServices": {
    "highlightSimple": {
      "description": "Exposes Highlight Simple Events",
      "versions": {
        "1.0.0": "provideHighlightSimple"
      }
    }
  }
}
